apiVersion: v1
kind: Service
metadata:
  name: student-app-service
spec:
  type: NodePort
  selector:
    app: student-app
  ports:
  - protocol: TCP
    port: 6000        # service port
    targetPort: 3000  # app port inside pod
    nodePort: 30080   # external NodePort
